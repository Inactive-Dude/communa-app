# ============================================================
# application.properties is intentionally empty.
# All configuration lives in application.yml
# Profile-specific overrides: application-dev.yml / application-prod.yml
# ============================================================